<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net5.0</TargetFramework>
    <RootNamespace>TepcoOutageAreaChecker</RootNamespace>
    <PackageId>$(AssemblyName.Split('-')[0])</PackageId>
    <Company>Explorers of the Binary World</Company>
    <Product>$(AssemblyName.Split('-')[0])</Product>
    <Authors>Explorers of the Binary World</Authors>
    <AssemblyName>$(AssemblyName.Split('-')[0])</AssemblyName>
  </PropertyGroup>

  <Import Project="..\TepcoOutageAreaChecker-shared\TepcoOutageAreaChecker-shared.projitems" Label="Shared" />

  <ItemGroup>
    <PackageReference Include="TepcoOutageSharp" Version="0.24.8.1713-beta-0530" />
  </ItemGroup>

</Project>
